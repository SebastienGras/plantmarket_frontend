import { Box, Typography } from "@mui/material";

const AddProduct = () => (
  <Box>
    <Typography variant="h5" gutterBottom>
      Ajouter un produit
    </Typography>
    <Typography>Formulaire d'ajout ici...</Typography>
  </Box>
);

export default AddProduct;
